## Data Generator

Desgined to be a containerized application to generate the data feeds which mimic the raw data from source systems

### Docker run command from project root directory

```bash
docker build -f data_generator/dockerfile -t data-generator-image .
```